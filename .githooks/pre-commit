#!/bin/sh
# Staged content, and the ways out of this directory that are not git.
#
# The message is checked by commit-msg, which runs after git has collected it.
# Never bypass with --no-verify: if this fires it is right, and the fix is to
# rewrite the text.
set -e
./scripts/check-no-personal-figures.sh --staged
./scripts/check-no-personal-figures.sh --build-context
